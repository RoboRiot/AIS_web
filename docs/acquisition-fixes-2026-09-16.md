# Acquisition fixes - September 16, 2026

## Live Google Ads changes

- CT Trailers: added phrase negatives `horse trailer`, `horse trailers`; exact negatives `trailer rental ct`, `trailers for rent ct`, `trailer rental in ct`, `trailer rental danbury ct`, `trailer rental stamford ct`, `mobile ct scan near me`, `ct scan near me`.
- MRI Trailers: added exact negatives `mobile mri near me`, `mobile mri services near me`, `mri near me`, `mri scan near me`, `mobile mri scan near me`; phrase negatives `mri appointment`, `mri appointments`.
- Created primary Phone call lead action **Connected calls from ads (60s)**: calls from ads, minimum 60 seconds, count One, no invented monetary value, 30-day click window.
- Saved that action as the account default for call reporting. The existing account phone asset uses the account setting, has call reporting enabled and call recording disabled, and routes to 5595376851.
- Direct editing of the existing phone asset returned a Google Ads error twice. The account-default route saved successfully and was reopened to verify the selected action.
- Budgets unchanged: MRI $10/day plus CT $10/day. No additional campaign activated. No Display/Search Partners expansion.
- These settings enable measurement of future qualifying calls from ads, not retroactive lead attribution. A real qualifying call is still needed to validate reporting end to end. Website phone-link clicks are not completed calls.

## Website changes awaiting deployment

- Shorter B2B trailer introduction and immediate availability CTA; facility-vs-patient distinction is explicit.
- Compact inquiry form before model lists and long content. On mobile, the photo gallery follows the form.
- Gallery no longer advances automatically; thumbnails are in a collapsible section. Trailer entrance fade removed. Phone remains 5595376851.
- Original SEO copy, headings, pricing qualifications, canonical metadata, FAQ and schema retained.
- Permanent `/product/:slug` to `/products/:slug` redirects for old WordPress URLs.
- Explicit description labels recover missing historical identifiers; nine exact ID/PN pairs restore verified GE/CT metadata. No visibility, image, availability, or inventory flags are overridden.
- Historical product aliases include name/PN variants and verified MX200, VCT interface-board, and relay URLs. Ambiguous names still fail closed.
- Product specification parsing now stops at the next field label even after whitespace normalization. This prevents manufacturer/category text leaking into part numbers and canonical URLs.
- Read-only reconciliation utility: `node scripts/audit-acquisition.mjs YYYY-MM-DD YYYY-MM-DD`. UTC date boundaries; aggregate lead output excludes inquiry text, contact data and click IDs. First-party 404 event counts are not unique visitors.

## Catalog evidence and remaining decisions

The GE metadata repairs are grounded in `data/CT/GE/final-CT-GE.json` and its GE HealthCare, PartsSource and Block Imaging source records for 2378286, 2326523, 5120815, 2266521-4, 5116570, 2137958-3, 5136400-3, 2318808-3 and 2256729. The guard requires both the historical document ID and exact original PN. Explicit OEM/modality fields win. A separate researched ADC2 repair corrects the known incomplete PN only for its exact ID and matching description.

The September 12-15 audit found 20 distinct failing product paths. All now have guarded recovery paths: 18 permanent redirects to matching current product records and two identifier-based selection pages. Follow-up research is recorded in `product-url-research-2026-09-16.md`:

- `/products/opconta-px79`: presents separate PX79-11179 and PX79-08650 choices, not an arbitrary redirect. Their existing image files are logos; no thumbnails or paid ads use these records.
- `/products/bsx73-e-adc2-board`: full BSX73-0977E Toshiba/CT identifiers confirmed against the original description and external part catalogs; exact-record repair restores the canonical redirect.
- `/products/toshiba-cxb-400c-ct-tube`: original item `temp-296` was found with null PN, an en dash in its title and five image paths. Exact-ID/title repair restores CXB-400C and its canonical redirect.
- `/products/mrc-rot-gs`: displays the currently public exact part-number match, with no assumption that all MRC ROT-GS tubes are interchangeable.

## Measurement findings

Read-only mail metadata audit for August 19-September 15 UTC: 23 accepted lead records, all 23 with successful email delivery; 16 parts, 6 contact, 1 trailer form. Sources: 17 Google organic, 5 direct, 1 other organic. None contained an ad click ID. This does not justify importing them as paid conversions or assuming every email was generated by an ad.

September 12-15 UTC: six accepted records, all delivered, five organic and one direct, all unreviewed. Staff still need to classify qualified opportunities, spam, quoted jobs and sales. No customer records or qualifications were modified.

## Verification and rollout

- Unit/regression tests include identifier guardrails, flattened specifications, ambiguous aliases, lead deduplication, signed-session recovery and privacy filtering.
- Production build, desktop/mobile screenshots, gallery interaction and overflow checks performed locally.
- All 64 unit/regression tests pass. The final production build and local browser suite pass after the researched URL repairs; 18 redirects work cold and warm, two variant selectors return 200 with noindex, and the three additional WordPress paths reach canonical products.
- `scripts/verify-lead-tracking.mjs` tests real local redirect responses cold/warm and local-only browser submissions at 1440px and 390px. All outbound test analytics/inquiries are intercepted; no test lead is sent to production.
- Browser scenarios cover paid trailer entry -> contact inquiry, stale timer recovery, lost response/retry, rejected submissions, one accepted GA4 lead per inquiry, and no PII/click ID in the GA4 event payload.
- Deploy the normal production release, then smoke-test the CT/MRI pages and representative redirects. This task does not push or deploy code.

## Parts experiment launch status

The user approved committing the changes and launching the $5/day parts test. Google Ads draft `10214322612` was created as **Parts - Selected GE Part Numbers**. Google then required account identity verification before saving the changed ad destination. It has not been published; no new spend is active. Complete verification, finish all three groups, verify the budget/goals/settings, reduce MRI to $5/day, then publish. `parts-search-test-draft.json` records the planned exact-part-number ads for 2222564-30-H, 2258273 and 5146634-5. Ad copy requests quotes and current availability, without inventory or delivery guarantees.

All three proposed live landing pages resolve successfully and were visually checked with real product photographs on September 16. The MDAS URL currently redirects to the existing long canonical URL; deploying this commit corrects its parsed part number and canonical destination. No inquiry was submitted to production during these checks.

Approved launch allocation: CT $10/day, MRI $5/day, parts $5/day, preserving the $20/day total. Do not activate the parts campaign without reducing MRI first. Review search terms, qualified inquiries, connected calls and cost per qualified lead; do not treat clicks or form openings as leads.
